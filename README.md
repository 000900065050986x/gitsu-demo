# GitSU demo

The `index.html` currently on this repo is a size-stripped preview stub.
The real self-contained demo (stacked tabs, KiT boot art, icon pickers, edge chip, 3x3 tools) is the `GitSU.html` file delivered in chat.
